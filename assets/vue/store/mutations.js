export default {
    SET_VEHICLE_TYPES(state, data) {
        state.types = data
    },
    SET_MAKES(state, data) {
        state.makes = data
    },
    SET_MODELS(state, data) {
        state.models = data
    }
}
